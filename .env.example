# Required: Primary secret key for server-side API Key encryption
ENCRYPTION_SECRET=YOUR_REAL_RANDOM_SECRET

# Optional: Legacy secret key used only for decrypting older Firestore records during migration
LEGACY_ENCRYPTION_SECRET=
